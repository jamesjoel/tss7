// ----- single line commant

/*
JavaScript is a case-sencentive lag.

-------- Variables in Node (JavaScript)

the variables are used for storing or holding information/data.


Roules for Nameing a Variables

1. The var name can not strat with number.
(7a, 10b, 5c --- not used     but a7, b10, c5 allowed)

2. Tha var name can not used any spacial char.
(#, @, $, %, ^, &, *, space not used but _ accapted)

3. var name also case-sencative
( var a,    var A )


*/

var a = 100;
var b = 200;

var c = a+b;  
console.log(c);